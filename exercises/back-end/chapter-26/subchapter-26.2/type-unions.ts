type StateOfMatter = 'Gas' | 'Liquid' | 'Plasma' | 'Solid';

const myDrinkStateOfMatter: StateOfMatter = 'Liquid';

/* ***************** */

type PersonalId = string | number;

const myId = '000000000-00';

/* ***************** */

type OS = 'Linux' | 'Mac' | 'Windows';

const favoriteOs: OS = 'Linux';

/* ***************** */

type Vowel = 'a' | 'e' | 'i' | 'o' | 'u';

const firstVowelInMyName: Vowel = 'e';
